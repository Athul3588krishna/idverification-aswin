/**
 * routes/verificationRoutes.js
 * ---------------------------------------------------------
 * OCR-based ID verification routes.
 * Full implementation will be added in Phase 4.
 */

const express = require("express");
const router = express.Router();

// Placeholder – will be replaced with real controller in Phase 4
router.get("/", (req, res) => {
  res.json({ message: "Verification routes ready. Phase 4 implementation pending." });
});

module.exports = router;
